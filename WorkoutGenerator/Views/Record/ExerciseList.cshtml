@model WorkoutGenerator.ViewModels.ViewExerciseListViewModel
<div class="container">
    <h2>@Model.Workout.Name</h2>
    <ul>
        @foreach (var exercise in Model.Exercises)
        {
            <li>
                <a asp-controller="Record"
                   asp-action="AddRecord"
                   asp-route-ExerciseID="@exercise.ExerciseID"
                   asp-route-WorkoutID="@Model.Workout.WorkoutID">@exercise.Name</a>
                @*TODO Currently Lists Workout name over and over*@
            </li>
        }
    </ul>
</div>